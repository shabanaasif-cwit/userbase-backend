# v0.2.9 — 2026-04-27

(D) 2026-04-27

### Added

-

### Changed

- **Static page ping routes removed**: Deleted **`staticPages.routes.js`** and unmounted it from **`app.js`**, so **`GET /gallery`**, **`GET /contact`**, and **`GET /about`** are no longer served as lightweight JSON markers for SPA route logging.

- **OpenAPI**: Removed **`StaticRouteResponse`** and documentation for **`/gallery`**, **`/contact`**, and **`/about`**. **`NavigationReportBody`** examples and descriptions now use paths like **`/dashboard`** and **`/settings`** instead of gallery/contact/about.

- **`uiInteractionLogger`**: For admin notification **DELETE**, only logs **Notification Deleted** when the response is **204**; the separate **Notification Delete Clicked** line was removed.

### Fixed

-

### Security (if applicable)

-

### Internal

- **`test/http.integration.test.js`**: Navigation POST integration examples updated to match the new OpenAPI samples; removed the **`static routes`** **`GET`** assertions for gallery/contact/about.
