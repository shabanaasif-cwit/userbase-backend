(D) 2026-05-06

### Added
Server log line to print POST/PATCH request payloads (with redaction for password, token, accessToken, refreshToken) in src/middleware/uiInteractionLogger.js.

### Changed
-
### Fixed
-

### Security (if applicable)
Redacted common secret fields from logged payloads to avoid leaking credentials/tokens in logs.

### Internal
Added helper safePayloadForLogging() to keep payload logging scoped and consistent.