# v0.2.3

(D) 2026-04-14

### Added

- `firstName` and `lastName` on each user in `GET /api/users` (`items[]`), and on the `user` object returned from admin `PATCH /api/users/:userId`, `PATCH /api/users/:userId/deactivate`. 
- `PATCH /api/users/:userId/toggle-account`, so clients can show a display name instead of duplicating email.

### Changed

- OpenAPI `User` component schema now includes `firstName`and `lastName` (aligned with auth responses and admin user payloads).

### Fixed

-

### Security (if applicable)

-

### Internal

-
