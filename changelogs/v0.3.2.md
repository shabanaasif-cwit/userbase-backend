(D) 2026-05-05

### Added
- Added `src/realtime/socketHub.js` with Socket.IO setup, JWT-based socket auth, user/role room joins, and emit helpers for notification/reminder events.
- Added real-time event emission for notification create/update/delete/read and reminder create/read flows from route handlers.

### Changed
- Updated server bootstrap in `src/index.js` to use `createServer(app)` and initialize Socket.IO on the same HTTP server.
- Updated notification and reminder routes to publish socket events after successful actions.

### Fixed
- Updated `deleteNotification` to return sanitized deleted notification data so `notification:deleted` events can emit consistent payloads.

### Security (if applicable)
- Added authenticated socket connection flow using bearer token verification before allowing real-time connections.

### Internal
- Added `socket.io` dependency in `package.json` and refreshed `package-lock.json`.
- Exported `recipientStateFingerprint` from `notificationService` for broader reuse.
