PORT=3001
MONGODB_URI=mongodb://127.0.0.1:27017/userbase
# Browser app URL (CORS + future httpOnly cookies). Use your real frontend origin in production.
FRONTEND_ORIGIN=http://localhost:5173

# Auth (add when implementing JWT module — do not commit real values)
# JWT_ACCESS_SECRET=
# JWT_REFRESH_SECRET=
# ACCESS_TOKEN_TTL=15m
# REFRESH_TOKEN_TTL=7d
